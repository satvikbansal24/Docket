{
  "name": "docket",
  "private": true,
  "description": "Docket — personal time & billing tracker. Dependencies here are for the netlify/functions/ask-ai.js serverless function only; the app itself is plain static HTML/JS with no build step.",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0",
    "zod": "^4.0.0"
  }
}
